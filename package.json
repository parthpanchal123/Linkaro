{
  "name": "linkaro",
  "version": "1.0.1",
  "description": "A premium, cloud-synced Chrome extension to store, manage, and share your links.",
  "packageManager": "pnpm@10.33.0",
  "scripts": {
    "build": "cross-env BUILD_ENTRY=firebase-bundle vite build && cross-env BUILD_ENTRY=auth-bundle vite build",
    "build:app": "cross-env BUILD_ENTRY=firebase-bundle vite build",
    "build:auth": "cross-env BUILD_ENTRY=auth-bundle vite build"
  },
  "keywords": ["chrome-extension", "links", "bookmarks", "firebase"],
  "author": "Parth Panchal",
  "license": "ISC",
  "dependencies": {
    "firebase": "^12.11.0"
  },
  "devDependencies": {
    "cross-env": "^7.0.3",
    "vite": "^8.0.6"
  }
}
